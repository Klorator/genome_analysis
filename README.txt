This is the versioned folder containing scripts for the genome analysis course.

Folder structure on UPPMAX:

genome_analysis
|
|---- scripts (such as .batch files for SLURM)
|---- .gitignore (ignores results folder)
|---- results (data files generated by the scripts)
      |
      |---- data files (data generated or symbolic links to Gorilla)